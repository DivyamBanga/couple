// Bump APP_VERSION on every deploy (and SW_VERSION in sw.js — keep them in lockstep).
// Bump PROTO_VERSION only when the P2P message protocol changes incompatibly.
export const APP_VERSION = '2026.08.28-1';
export const PROTO_VERSION = 1;
